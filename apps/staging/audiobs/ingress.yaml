apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: placeholder
  namespace: placeholder
spec:
  ingressClassName: traefik
  rules:
    - host: placeholder.d11s.space
      http:
        paths:
          - backend:
              service:
                name: placeholder
                port:
                  number: 9090
            path: /
            pathType: Prefix
